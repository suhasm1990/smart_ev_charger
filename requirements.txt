# Core runtime
requests>=2.33.0
schedule>=1.2.2
python-dotenv>=1.2.0
tzdata>=2026.1
# Required at runtime by schedule.every().day.at(time, tz) — imported lazily
# inside schedule, so it never appears as a direct import in this codebase.
pytz>=2026.1

# Hardware & cloud integrations
python-chargepoint>=2.3.2
gspread>=6.2.0
google-auth>=2.30.0

# Telegram bot & model-agnostic LLM access
pyTelegramBotAPI>=4.34.0
litellm>=1.60.0

# Monthly bill infographic
matplotlib>=3.10.0
